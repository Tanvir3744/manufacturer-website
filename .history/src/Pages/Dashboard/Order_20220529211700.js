import React, { useState } from 'react';

const Order = () => {
    const [orders, setOrders] = useState({})
    return (
        <div>
            this is my order
        </div>
    );
};

export default Order;